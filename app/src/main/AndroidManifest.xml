<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The home-screen contribution card fetches the contributor list from the GitHub API. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Entering or leaving silent counts as a Do Not Disturb change, so the ringer-cycle action
         needs this declared for the app to appear under Settings' Do Not Disturb access list. -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Required to bind Nothing's Glyph Matrix service for the Glyph light action. -->
    <uses-permission android:name="com.nothing.ketchum.permission.ENABLE" />

    <!-- The action picker lists launchable apps, so package visibility must be declared. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>

        <!-- The unlock wizard checks these Nothing consumer packages' enabled state directly, so
             each must be visible under Android 11+ package-visibility rules. -->
        <package android:name="com.nothing.ntessentialspace" />
        <package android:name="com.nothing.ntessentialrecorder" />
        <package android:name="com.essentialintelligence" />

        <!-- The Glyph Matrix SDK binds com.nothing.thirdparty/.GlyphService by action; without this
             the bind silently fails under Android 11+ package-visibility rules (no exception, no
             ServiceConnection callback at all). -->
        <intent>
            <action android:name="com.nothing.thirdparty.bind_glyphservice" />
        </intent>
    </queries>

    <application
        android:name=".EssentialKeyToolsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EssentialKeyTools">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.EssentialKeyTools">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.EssentialKeyDetectionService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>

</manifest>
